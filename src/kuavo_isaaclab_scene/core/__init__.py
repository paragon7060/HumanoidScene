"""Shared package paths and runtime compatibility checks.

Modules are intentionally not imported here: Isaac-dependent modules must be
loaded only after AppLauncher has initialized the simulator.
"""
