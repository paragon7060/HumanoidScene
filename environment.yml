name: env_isaaclab_232
channels:
  - defaults
dependencies:
  - python=3.11
  - pip

# Isaac Sim and Isaac Lab are intentionally not listed here. Install them from
# NVIDIA's index/source with ./install_isaaclab_stable.sh so their exact
# versions and the CUDA-enabled PyTorch index are handled correctly.
