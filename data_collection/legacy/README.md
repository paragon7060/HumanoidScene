# Task1 legacy history

This directory is a history-only snapshot of superseded Task1 drafts and tools.
It is intentionally not a Python package and is excluded from the supported
runtime and tests. Do not import or execute files from here; use
`data_collection.task1` and the supported wrappers under `scripts/`.

The files remain versioned so earlier experiments can be audited against their
original source.
